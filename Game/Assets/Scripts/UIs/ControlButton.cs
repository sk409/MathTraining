using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class ControlButton : AppButton
{

    protected override void Start()
    {
        base.Start();
        //var seControl = GameObject.Find("SEControlButton");
        //onClick.AddListener(() =>
        //{
        //    seControl.GetComponent<AudioSource>().Play();
        //});
    }

}
