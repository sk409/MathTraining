using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpecialButton : AppButton
{
    protected override void Start()
    {
        base.Start();
        //onClick.AddListener(() =>
        //{
        //    SoundWareHouse.Instance.seSpecialButton.GetComponent<AudioSource>().Play();
        //});
    }
}
